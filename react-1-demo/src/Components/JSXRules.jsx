import React from 'react'

const JSXRules = () => {
  return (
    <div>
      <h3>JSX rules</h3>
      <p>JSX must return a single paremt element</p>
      <p>JSX elements must be properly closed</p>
      <p>JSX attribute are written using camelCase(e.g., className instead of class).</p>
    </div>
  )
}

export default JSXRules
